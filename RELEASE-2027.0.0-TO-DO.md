# Release 2027.0.0 — To Do

This file is a focused, actionable checklist to upgrade the repository to `release-2027.0.0`.

## Summary
- Base: `prod-2027.0.0` (current canonical branch in this fork).
- Goal: create `release-2027.0.0`, update version markers, ship changelog/release notes, run tests/CI, merge and tag `v2027.0.0`.

---

## Checklist

- [ ] Ensure clean working tree
  - Run `git status --porcelain` and stash or commit any WIP before proceeding.

- [ ] Fetch and sync remote refs
  - `git fetch --prune origin` — make sure `origin/prod-2027.0.0` is up-to-date.

- [ ] Create release branch `release-2027.0.0`
  - `git checkout -b release-2027.0.0 origin/prod-2027.0.0`

- [ ] Find and update version markers
  - Update `VERSION` to `2027.0.0` and search for occurrences of older version strings (e.g., `2026.2.0`) and update as appropriate.
  - `git grep -n "2026.2.0" || true`

- [ ] Update changelog & release notes
  - Add a `release_notes/release-2027.0.0.md` and update `CHANGELOG.md` with key user-facing changes and migration notes.

- [ ] Run tests and linters
  - Activate the dev venv: `source .venv3.13.3/bin/activate` (if present).
  - Run tests: `python -m pytest -q`
  - Run linter(s): `ruff check .` (adjust to repo configuration).

- [ ] Update CI workflows if needed
  - Inspect `.github/workflows/*` for branch-specific filters; make updates so release workflows run appropriately.

- [ ] Commit version and doc changes
  - `git add VERSION CHANGELOG.md release_notes/` (plus any other changed files)
  - `git commit -m "chore(release): prepare release-2027.0.0 — bump VERSION and add release notes"`

- [ ] Push branch and open PR
  - `git push -u origin release-2027.0.0`
  - Open a PR from `release-2027.0.0` → `prod-2027.0.0` with release notes and checklist.

- [ ] Merge, tag, and create GitHub release
  - After PR approval and CI passing, merge in GitHub.
  - Tag locally: `git tag -a v2027.0.0 -m "Release v2027.0.0"`
  - Push tag: `git push origin v2027.0.0`
  - Create a GitHub Release with changelog and assets.

- [ ] Post-release housekeeping
  - Update docs and READMEs, notify stakeholders, bump any packaging manifests, and ensure CI artifacts are being uploaded.

- [ ] Optional: mirror to upstream or create backports
  - If this fork should be upstreamed, open PR(s) to the upstream repository or push the branch upstream per your contribution policy.

---

## Notes & Guidance
- Make small, testable commits and run the test suite after each logical change.
- Prefer opening a PR for review rather than pushing directly to `prod-2027.0.0` unless you are the release owner.
- If you hit merge conflicts when creating `release-2027.0.0`, resolve locally and run tests before pushing.

---

Generated by the release preparation assistant on 2025-11-12.
